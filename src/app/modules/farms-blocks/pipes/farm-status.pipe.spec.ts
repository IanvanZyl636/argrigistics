import { FarmStatusPipe } from './farm-status.pipe';

describe('FarmStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FarmStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
